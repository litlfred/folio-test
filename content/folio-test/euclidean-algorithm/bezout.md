For any integers $a$ and $b$, not both zero, there exist integers $x$ and $y$
with
$$ax + by = \gcd(a, b).$$

Moreover $\gcd(a,b)$ is the *smallest* positive integer expressible as
$ax + by$, and every integer of that form is a multiple of it.
